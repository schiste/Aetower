[graph]
targets = []

[advisories]
version = 2
ignore = [
  { id = "RUSTSEC-2025-0141", reason = "History snapshots are still persisted with bincode v1; removing it requires an explicit on-disk migration path rather than a silent dependency swap." },
]

[bans]
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
