[graph]
targets = []

[advisories]
version = 2
ignore = [
  { id = "RUSTSEC-2025-0141", reason = "bincode v1 still encodes history.db snapshot blobs in aetower-persistence; migration plan is tracked in docs/bincode-migration-plan.md." },
]

[bans]
multiple-versions = "warn"
wildcards = "allow"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
